import Vue from 'vue'
import Vuex from 'vuex'
import order from './module/order'

Vue.use(Vuex)

const store = new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    order
  }
})

export default store
